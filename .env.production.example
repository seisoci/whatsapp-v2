# Backend API Configuration
# Option 1: Use frontend URL (Next.js will proxy to backend via rewrites)
NEXT_PUBLIC_API_URL=https://yourdomain.com
NEXT_PUBLIC_API_PREFIX=/api/v1
BACKEND_URL=http://localhost:3001

# Option 2: Direct backend API (disable rewrites in next.config.mjs)
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_API_PREFIX=/api/v1
# BACKEND_URL=https://api.yourdomain.com

# Next.js App URL (Frontend)
# Port Configuration
NEXT_PORT=3000

# PostgreSQL Configuration (same as backend)
DB_HOST=your-db-host
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-password
DB_DATABASE=whatsapp

# Cloudflare Turnstile Configuration
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key

# WebSocket Configuration
# IMPORTANT: Use your production domain, protocol will auto-detect (wss:// for https)
NEXT_PUBLIC_WS_URL=api.yourdomain.com
